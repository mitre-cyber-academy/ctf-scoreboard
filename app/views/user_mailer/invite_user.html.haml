%p
  Hello #{@invite.email}!
%p
  You have been invited to join the team
  %b #{@team.team_name}
  for the upcoming #{@game.organization} #{@game.title}
- if @invite.user.nil?
  %p Click the link below to register an account to view the invitation.
  %p= link_to 'Create my account', new_user_registration_url(:email => @invite.email)
- else
  %p Click the link below to view the invitiation.
  %p= link_to 'View invitation', join_team_users_url
