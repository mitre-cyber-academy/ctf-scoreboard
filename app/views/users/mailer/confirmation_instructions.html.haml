%p
  = t('user_mailer.confirmation.greeting', fullname: @user.full_name)
  = t('user_mailer.confirmation.confirmation_line_1')
  %p= t('user_mailer.confirmation.confirmation_line_2')

%p= link_to t('user_mailer.confirmation.confirm_account'), confirmation_url(@resource, :confirmation_token => @token)

%p= t('user_mailer.confirmation.error')