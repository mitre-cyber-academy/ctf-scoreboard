%p
  = t('user.mail.confirmation.greeting', fullname: @user.full_name)
  = t('user.mail.confirmation.confirmation_line_1')
  %p= t('user.mail.confirmation.confirmation_line_2')

%p= link_to t('user.mail.confirmation.confirm_account'), confirmation_url(@resource, :confirmation_token => @token)

%p= t('user.mail.confirmation.error')