- content_for :header do
  Confirm your Account

%legend Resend confirmation instructions
= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post, :class => "user-reset-form"  }) do |user|
  = render "users/shared/retrieve_access", user: user
