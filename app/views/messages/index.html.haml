- content_for :header do
  Messages
- if @messages.count == 0
  .zero-items-text No Messages
- else
  - @messages.each do |m|
    %div.message
      %h2
        = m.title
        %small= m.updated_at.strftime("%b %e, %R %Z")
      = raw(BlueCloth.new(m.text).to_html)
  = paginate @messages
